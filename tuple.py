__author__ = 'hkc'

class tuple(object):
    def __init(self, action, obs, reward, info, done):
        self.action = action
        self.obs = obs
        self.reward = reward
        self.info = info
        self.done = done
